'use strict';

/**
 * excluded-author-site-mapping controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::excluded-author-site-mapping.excluded-author-site-mapping');
