'use strict';

/**
 * excluded-author-site-mapping router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::excluded-author-site-mapping.excluded-author-site-mapping');
