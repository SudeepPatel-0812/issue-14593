'use strict';

/**
 * excluded-author-site-mapping service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::excluded-author-site-mapping.excluded-author-site-mapping');
