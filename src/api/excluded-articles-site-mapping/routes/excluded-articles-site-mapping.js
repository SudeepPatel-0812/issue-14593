'use strict';

/**
 * excluded-articles-site-mapping router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::excluded-articles-site-mapping.excluded-articles-site-mapping');
