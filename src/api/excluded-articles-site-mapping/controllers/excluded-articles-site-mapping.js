'use strict';

/**
 * excluded-articles-site-mapping controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::excluded-articles-site-mapping.excluded-articles-site-mapping');
